nifi-master:
  ports:
  - 80:8081/tcp
  restart: always
  environment:
    PASSWORD: Pa22word
    ISMANAGER: 'true'
  labels:
    io.rancher.scheduler.affinity:host_label: nifi_master=true
  tty: true
  image: clemenko/nifi
  stdin_open: true
  net: host
